<UserControl x:Class="Pokno.Payment.Views.PaymentSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://www.codeplex.com/prism"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid x:Name="LayoutRoot" Background="#E7FDFDFD">
        <TabControl prism:RegionManager.RegionName="PaymentSetupTabRegion"
                    Name="tcPaymentSetupTab"
                    Grid.Row="1" 
                    Margin="5,0,0,0"
                    Width="Auto" 
                    Height="Auto" 
                    Style="{StaticResource TabControlStyle}" 
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}" 
                    TabStripPlacement="Top" 
                    
                    ItemContainerStyle="{StaticResource TabItemRegionStyle}"
            >
        </TabControl>



    </Grid>
</UserControl>
